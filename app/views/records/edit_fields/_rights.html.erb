<% options = RightsService.select_options.dup %>
<% options.unshift(['- Choose Creative Commons License -', nil]) %>
<%= f.input :rights, as: :multi_value_select_with_accessible_help,
    collection: options,
    input_html: { class: 'form-control', multiple: true }, force_single: true %>
