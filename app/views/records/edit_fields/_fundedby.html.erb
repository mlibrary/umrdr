<% options = FundedbyService.select_options.dup %>
<% options.unshift(['- Choose Funding Agency -', nil]) %>
<%= f.input :fundedby, as: :select_with_accessible_help, required: false,
    collection: options,
    input_html: { class: 'form-control', multiple: false } %>