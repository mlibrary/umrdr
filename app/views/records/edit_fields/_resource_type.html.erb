<%# would be select_with_accessible_help when we have +1 resource types (collection: Sufia.config.resource_types,) %>
<%= f.input :resource_type, as: :hidden, 
    input_html: { class: 'form-control', multiple: true } %>
